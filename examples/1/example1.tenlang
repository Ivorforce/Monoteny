// alias: floor_div
pattern {} /. {} :: MultiplicationPrecedence;

fun {lhs: $Number} /. {rhs: $Number} -> $Number :: lhs / rhs;

fun square(_ a: $Number) -> $Number :: a * a;
fun {self: $Number}.square() -> $Number :: self * self;

fun main() :: {
    let a = square(2.0) /. 5.0;

    var b = 5;
    b = b.square() ** 2;

    print("Test: ");
    print(a);
    print(b);
}
