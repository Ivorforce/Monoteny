import monoteny as mn
import numpy as np
import math
import operator as op
from numpy import int8, int16, int32, int64, int128, uint8, uint16, uint32, uint64, uint128, float32, float64, bool
from typing import Any, Callable


def main():
    """
    <Docstring TODO!>
    """
    print("Test: ")
    value = (square_1(Float32(2.2), Number=mn.declarations.Number_7)) * (Float32(np.e))
    b = (square_0(value=value, Number=mn.declarations.Number_7)) + (pi_ish_0(Float=mn.declarations.Float_1))
    b = (floor_div((square_1(b, Number=mn.declarations.Number_7)) ** (-(Float32(2.2))), value, Float=mn.declarations.Float_1)) + (Float32(np.pi * 2))
    c = pi_ish_1(Int=mn.declarations.Int_8)
    print(((b < (Float32(2))) and (not (value > (Float32(2))))) and (True))


if __name__ == '__main__':
    main()
